`etcd` = **/etc** + **distributed**
- `/etc`:  
    유닉스/리눅스 시스템에서 **설정 파일이 모여 있는 디렉토리**  
    → 시스템 구성(config)을 저장하는 위치
    
- `d`:  
    **distributed (분산형)**의 약자  
    → 분산 시스템에서 동작한다는 의미

etcd = 분산 환경에서 설정 정보를 저장하는 /etc 역할을 하는 시스템