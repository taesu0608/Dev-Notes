
# 1.시그니처(Signature) 는  
메서드가 "다른 메서드인지"를 **구분 짓는 기준**
- **메서드 이름 + 매개변수 목록(타입, 수, 순서)**
    
- 반환 타입은 시그니처에 포함되지 않음  
    → 반환값이 다르다고 해서 오버로딩 불가

> 보통 `시그니처`라는 말을 사용하는 문맥은:  
    “같은 메서드명” 아래에서의 오버로딩 판단 상황
그래서 실질적으로는:
	"매개변수의 **타입, 수, 순서**가 오버로딩을 구분하는 핵심"  